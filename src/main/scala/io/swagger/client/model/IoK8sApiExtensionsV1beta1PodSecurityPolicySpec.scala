/**
 * Kubernetes
 * No description provided (generated by Swagger Codegen https://github.com/swagger-api/swagger-codegen)
 *
 * OpenAPI spec version: v1.9.0
 * 
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 * Do not edit the class manually.
 */

package io.swagger.client.model


case class IoK8sApiExtensionsV1beta1PodSecurityPolicySpec (
  /* AllowPrivilegeEscalation determines if a pod can request to allow privilege escalation. */
  allowPrivilegeEscalation: Option[Boolean],
  /* AllowedCapabilities is a list of capabilities that can be requested to add to the container. Capabilities in this field may be added at the pod author's discretion. You must not list a capability in both AllowedCapabilities and RequiredDropCapabilities. */
  allowedCapabilities: Option[List[String]],
  /* is a white list of allowed host paths. Empty indicates that all host paths may be used. */
  allowedHostPaths: Option[List[IoK8sApiExtensionsV1beta1AllowedHostPath]],
  /* DefaultAddCapabilities is the default set of capabilities that will be added to the container unless the pod spec specifically drops the capability.  You may not list a capabiility in both DefaultAddCapabilities and RequiredDropCapabilities. */
  defaultAddCapabilities: Option[List[String]],
  /* DefaultAllowPrivilegeEscalation controls the default setting for whether a process can gain more privileges than its parent process. */
  defaultAllowPrivilegeEscalation: Option[Boolean],
  /* FSGroup is the strategy that will dictate what fs group is used by the SecurityContext. */
  fsGroup: IoK8sApiExtensionsV1beta1FSGroupStrategyOptions,
  /* hostIPC determines if the policy allows the use of HostIPC in the pod spec. */
  hostIPC: Option[Boolean],
  /* hostNetwork determines if the policy allows the use of HostNetwork in the pod spec. */
  hostNetwork: Option[Boolean],
  /* hostPID determines if the policy allows the use of HostPID in the pod spec. */
  hostPID: Option[Boolean],
  /* hostPorts determines which host port ranges are allowed to be exposed. */
  hostPorts: Option[List[IoK8sApiExtensionsV1beta1HostPortRange]],
  /* privileged determines if a pod can request to be run as privileged. */
  privileged: Option[Boolean],
  /* ReadOnlyRootFilesystem when set to true will force containers to run with a read only root file system.  If the container specifically requests to run with a non-read only root file system the PSP should deny the pod. If set to false the container may run with a read only root file system if it wishes but it will not be forced to. */
  readOnlyRootFilesystem: Option[Boolean],
  /* RequiredDropCapabilities are the capabilities that will be dropped from the container.  These are required to be dropped and cannot be added. */
  requiredDropCapabilities: Option[List[String]],
  /* runAsUser is the strategy that will dictate the allowable RunAsUser values that may be set. */
  runAsUser: IoK8sApiExtensionsV1beta1RunAsUserStrategyOptions,
  /* seLinux is the strategy that will dictate the allowable labels that may be set. */
  seLinux: IoK8sApiExtensionsV1beta1SELinuxStrategyOptions,
  /* SupplementalGroups is the strategy that will dictate what supplemental groups are used by the SecurityContext. */
  supplementalGroups: IoK8sApiExtensionsV1beta1SupplementalGroupsStrategyOptions,
  /* volumes is a white list of allowed volume plugins.  Empty indicates that all plugins may be used. */
  volumes: Option[List[String]]
)

